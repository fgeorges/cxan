# For HTTP
NameVirtualHost *:80

<VirtualHost *:80>

    ServerName test.cxan.org

    ### Reporting ###
    ErrorLog  /home/cxan/test/apache/error.log
    # Possible values: debug, info, notice, warn, error, crit, alert, emerg.
    LogLevel  debug
    CustomLog /home/cxan/test/apache/access.log combined
    ServerSignature On

    ### Redirect to Servlex ###
    ProxyRequests     Off
    ProxyPreserveHost On
    ProxyPass         / http://localhost:19457/cxan/
    ProxyPassReverse  / http://localhost:19457/cxan/
    <Proxy *>
        Order deny,allow
        Allow from All
    </Proxy>

</VirtualHost>
